[phases.setup]
nixPkgs = ["python311", "ffmpeg-full", "wget", "curl"]

[phases.install]
cmds = [
    "python -m venv --copies /opt/venv",
    ". /opt/venv/bin/activate && pip install --upgrade pip setuptools",
    ". /opt/venv/bin/activate && pip install -e ."
]

[phases.build]
cmds = ["mkdir -p uploads processed /tmp/n8n/simple_video"]

[start]
cmd = ". /opt/venv/bin/activate && gunicorn --bind 0.0.0.0:$PORT --timeout 600 app:app"